<%= form_with url: search_path, method: :get, local: true, class: 'search-container' do |f| %>
  <%= f.text_field :content, placeholder: '楽団を検索する' %>
  <%= f.select :model, options_for_select({ "Team" => "team", "パート" => "tag" }) %>
  <%= f.submit '検索', class: 'search-btn' %>
<% end %>